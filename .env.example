# Copy this file to `.env` and fill in real values.
# Never commit `.env` — it's already in .gitignore.

# --- Public (exposed to the client bundle, prefixed PUBLIC_) ---
PUBLIC_SITE_NAME="Smart Workflow Lab"
PUBLIC_SITE_URL="https://smartworkflowlab.com"

# --- Server-only secrets (never sent to the browser) ---
API_BASE_URL=""
DATABASE_URL=""
